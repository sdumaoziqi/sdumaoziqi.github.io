

## 安装

https://cloud.tencent.com/developer/article/2309562

## 加root权限
```
sudo groupadd docker
sudo gpasswd -a ${USER} docker
sudo systemctl restart docker
sudo chmod a+rw /var/run/docker.sock

```


## docker - 更换国内加速镜像源
修改配置文件，没有则新增这个文件 `vi /etc/docker/daemon.json`

内容修改为
```json
{
  "registry-mirrors": [
    "https://hub.xdark.top",
    "https://hub.littlediary.cn",
    "https://dockerpull.org",
    "https://hub.crdz.gq",
    "https://docker.1panel.live",
    "https://docker.unsee.tech"
  ]
}
```
里面的地址可以随意更换，有一个可用就行

wq保存退出后，执行`sudo systemctl daemon-reload && sudo systemctl restart docker`

执行`docker info`查看`Registry Mirrors`是否生效

 

最新镜像地址可看阿里社区的帖子
https://cloud.tencent.com/developer/article/2459822
https://www.coderjia.cn/archives/dba3f94c-a021-468a-8ac6-e840f85867ea

 